Mobile First approach demo
==========================

First of all, we load mobile styles. Then schedule loading of desktop
styles as soon as media match triggers on desired query for the first time.
We can load scripts as well.
